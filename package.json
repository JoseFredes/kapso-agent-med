{
  "name": "kapso-agent-med",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.10.6",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}